/**
 * Auth layout - excludes global Header/Footer
 * Auth pages have their own custom layout
 */
export default function AuthLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
